# Radsvinn State Template

Starting shape for per-initiative state files in `tasks/{slug}/`.

See `.claude/templates/STATE.md` for full examples. This template is a Codex companion and must stay in sync.
