{
  "name": "radsvinn-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Radsvinn dashboard BFF — GitHub-OAuth-gated browser client for the Radsvinn planner API, with zero npm runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
